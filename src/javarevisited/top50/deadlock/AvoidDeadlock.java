package javarevisited.top50.deadlock;

public class AvoidDeadlock {

    /**
     * avoid nested synchronized block
     * not opposite ordered to same order or avoid calling one synchronized method from other,
     */
}
