package javarevisited.top50.deadlock;

public class DetectDeadlock {
    /**
     * I see a nested synchronized block or
     * calling one synchronized method from other,
     * or trying to get a lock on a different object then there is
     * a good chance of deadlock if a developer is not very careful.
     */
}
